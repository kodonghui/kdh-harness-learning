# Default learner profile template.
# 기본 학습자 프로필 템플릿.
#
# On first run, /kdh-study creates `profile.local.yaml` from these defaults.
# Edit `profile.local.yaml` (gitignored) for your personal state — do NOT edit this file.
#
# 최초 실행 시 /kdh-study가 이 값들로 `profile.local.yaml`을 생성합니다.
# 개인 상태는 `profile.local.yaml`(gitignore)에서 수정하세요 — 이 파일은 건드리지 마세요.

level: beginner           # beginner | intermediate | advanced
interests:
  - agent-harness
  - ai-tools
session_length: 5min      # preferred session length
total_sessions: 0
total_concepts_learned: 0
created: ""               # ISO date, filled on first run
